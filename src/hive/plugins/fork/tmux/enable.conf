bind % run-shell -b "hive fork --pane #{pane_id} -s h >/dev/null 2>&1 || tmux split-window -h -c '#{pane_current_path}'"
bind '"' run-shell -b "hive fork --pane #{pane_id} -s v >/dev/null 2>&1 || tmux split-window -v -c '#{pane_current_path}'"
